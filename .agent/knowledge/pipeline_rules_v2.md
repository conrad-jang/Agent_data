# 🚀 도파민연구소 쇼츠 자동화 파이프라인 (v2.0 업데이트)
**업데이트 일자:** 2026년 3월 24일
**문서 목적:** 에이전트(기대리, 문작가, 편집감독)의 변경된 스킬과 파이프라인 규칙 영구 보존

---

## 1️⃣ [기대리] 숏폼 소싱 및 필터링 규칙
- **숏폼 강제화**: 영상 소싱 시 구도의 깨짐을 방지하기 위해 가로형 롱폼은 절대 배제하며, **반드시 세로형(9:16) 60초 이하의 유튜브 숏츠(Shorts) 또는 틱톡 영상만** 골라냅니다.
- **파일명 보정**: Windows CMD `cp949` 환경의 렌더링 에러를 막기 위해, 유튜브 제목에서 **이모지 및 특수문자를 완벽하게 제거**한 영문/숫자 기반의 안전한 파일명으로 다운로드합니다.
- **수동 소스 처리**: 사용자가 `original/useroriginal/[yy.mm.dd]` 경로에 직접 넣어준 영상 역시 파이프라인에서 정상적으로 인식하고 작업에 착수해야 합니다.

## 2️⃣ [문작가] 고품격 시네마틱 대본 작성 가이드
- **시네마틱 템포 (자연/힐링 영상)**: 
  - 대사가 너무 빽빽하면 품질이 낮아 보입니다. 중간에 반드시 **"잠시 감상해보실까요?"** 라는 지시어를 삽입합니다. 이 멘트 이후에는 시스템이 자동으로 3초의 침묵(오디오/자막 여백)을 추가하여 시청자가 영상 자체에 몰입할 수 있게 합니다.
- **표준 아웃트로**: 
  - 자극적인 구독 유도 멘트를 지양하고, **"구독하시고 재미있는 영상을 받아보세요."**와 같이 신뢰감을 주는 노멀한 톤으로 통일합니다.
- **대본 파일 경로 및 작성일자 규칙**:
  - 생성된 모든 마크다운 대본은 원본 영상 폴더와 분리하여 **무조건 `D:\AI Porject\shorts_factory\script\` 폴더 내에 저장**합니다.
  - 마크다운 내용 최상단에 언제 쓰였는지 파악할 수 있도록 **`[작성일: YYYY-MM-DD]`** 형식으로 작성일을 기재합니다.
  - 파일명 자체는 원본 폴더 내에 있는 원본 영상의 파일명 원형과 100% 동일한 `.md` 파일명을 사용합니다. (이렇게 해야 편집감독이 파이프라인에서 파일명 하나로 두 폴더 간 동적 매핑이 가능합니다)

## 3️⃣ [편집감독] 프리미엄 렌더링 엔진 작동 방식 (`make_dopamine.py`)
- **강제 숏폼 크롭 (`subclipped`)**:
  - 원본이 1분을 초과하는 롱폼 영상이 들어와도, 시스템 메모리 낭비와 포맷 파괴를 막기 위해 **로드 직후 즉시 최대 59초로 강제 크롭(자르기)** 처리합니다.
- **프리미엄 TTS (Typecast AI) 연동**:
  - 실행 명령어에 `--typecast` 플래그를 추가하면, 기존 gTTS 대신 프리미엄 **타입캐스트 API**를 호출합니다.
  - **보이스 설정**: `'새론' (tc_65c47f4f7e237f1cb0a80380)` 보이스에 `ssfm-v21` 모델 적용.
  - **속도 튜닝**: API 페이로드에 `"tempo": 1.0`을 담아 음성 변환을 요청하고, 영상 배속 역시 1.0배속(정상 속도)으로 맞추어 저렴해 보이는 느낌을 완벽 차단합니다.
  - **오류 폴백**: 타입캐스트 호출 실패 또는 파라미터 에러 발생 시 자동으로 안전한 무료 텍스트 변환(gTTS)으로 대체하여 렌더링 실패를 막습니다.
- **파일명 생성 규칙 (동적 트래킹)**:
  - 출력되는 최종 결과물은 원본 영상명이 아닌 문작가의 '대본 타이틀(Title)'을 사용합니다.
  - 최종 포맷 규칙: **`[YYYYMMDD_HHMM]_마크다운_기획_제목.mp4`** (예: `[20260324_1730]_지구의_숨겨진_보석.mp4`)

## 4️⃣ [통합] 유튜브 자동 업로드 파이프라인 (`make_dopamine_perfect.py`)
- **자동 연동**: 영상 제작이 완료되는 즉시 `upload_to_youtube.py`를 호출하여 채널에 업로드합니다.
- **인증 방식**: OAuth2 로컬 서버 방식을 사용하며, 첫 실행 시 브라우저 인증 후 `token.pickle`을 생성하여 영구적으로 사용합니다.
- **업로드 설정**: 
  - 카테고리: 엔터테인먼트 (24)
  - 공개 범위: 기본값 `private` 또는 `unlisted` (안전을 위해)
  - 메타데이터: 문작가가 작성한 제목과 설명을 자동으로 매핑하여 업로드합니다.
- **실행 옵션**: `--upload` 플래그를 추가하여 제작과 동시에 업로드를 명령할 수 있습니다. 
  (예: `python make_dopamine_perfect.py 영상.mp4 대본.md --typecast --upload`)
